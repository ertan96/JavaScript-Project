class MovingObject {
    constructor(options) {
        this.pos = options.pos;
        this.vel = options.vel;
        this.game = options.game;
    }

    draw(ctx) {
        
    }

}

export default MovingObject;